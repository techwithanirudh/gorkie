// The prompt teaches these, and the step guard retries a step made only of
// lines opening with one, so a marker the model is told to use must be one the
// guard recognises.
export const reasoningMarkers = ['→', '↺', '?', '●', '◐', '○', '⚠'];
