--- a/dist/chunk-B3SHPOO6.js
+++ b/dist/chunk-B3SHPOO6.js
@@ -3099,6 +3099,10 @@
                 }
               }
             }
+          } else if (part.type === "file") {
+            const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+            tokenString += `${part.filename ?? ""}${mediaType}`;
+            overhead += mediaType.startsWith("image/") ? 765 : 258;
           } else {
             tokenString += JSON.stringify(part);
           }
--- a/dist/chunk-P6SA5MPH.cjs
+++ b/dist/chunk-P6SA5MPH.cjs
@@ -3131,6 +3131,10 @@
                 }
               }
             }
+          } else if (part.type === "file") {
+            const mediaType = typeof part.mediaType === "string" ? part.mediaType : "";
+            tokenString += `${part.filename ?? ""}${mediaType}`;
+            overhead += mediaType.startsWith("image/") ? 765 : 258;
           } else {
             tokenString += JSON.stringify(part);
           }
