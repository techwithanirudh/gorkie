# Template TODO

Only outstanding work lives here. Finished items move to
[IMPLEMENTED.md](./IMPLEMENTED.md) with their detail intact.

The sections above `## Product` are the reusable template: keep those limited to work that
belongs in anything built from this repo. Everything from `## Product` down is the gorkie
product backlog and is not expected to survive a fork.

> **Split note (2026-09-13, superseded 2026-09-24):** backgrounding and the trim-payloads span processor used to live on a separate `background` branch. Both are now on `chore/quality-pass` (98320648, a9a65778).

## CRITICAL (2026-09-13)

Nothing open.

## Owner decisions (accepted 2026-09-24)

Recommended defaults from the backlog triage, accepted with the owner's revisions. Each is also tagged on the items it unblocks. Items these closed are in IMPLEMENTED.md under 2026-09-24.

1. **GitHub in shared threads:** first decided DM-only (built in ebfdbafeb2), then REVERSED by the owner the same day: DMs by default, shared threads opt-in per user from App Home, for both GitHub and user MCP tools (e5588027). Focus mode shipped separately (4070081f).
2. **GitHub tool surface:** keep the SDK tools and the firewall-injected credential (no `gh` CLI, no `GH_TOKEN` in the sandbox). GitHub tools go behind `search_tools`; Slack core tools stay resident.
3. **Sandbox posture:** keep open egress plus `denyOut` for RFC1918, link-local and metadata ranges. Accept the MCP DNS-rebinding TOCTOU. Never put MCP servers or secrets in the sandbox.
4. **Working-model cache:** keep the in-process 5-minute non-renewing pin; no persistence.
5. **Feedback tool (revised):** keep the name `submit_feedback`, no ask-before-filing. Fix only its shape: value = kind, comment = body, unique id.
6. **Usage limits (revised):** per user 40 turns/hour and 300/day, moderators exempt, new small Postgres table, ephemeral notice when over, and remaining turns (hour and day) shown in App Home. Per-turn output cap of 200k tokens via an output processor.
7. **Backgrounding:** port the `background` branch now; background jobs capped at 60 min with a 10-min keepalive; `timeout` required; auto-convert-on-timeout only after the completion wake is live-verified. Confirm the E2B plan limit.
8. **Raw Slack API:** re-add `call_slack_api` with an exact read-only allowlist (`reactions.get`, `pins.list`, `bookmarks.list`, `usergroups.list`, `usergroups.users.list`, `users.getPresence`, `team.info`, `emoji.list`). No per-tool MCP overrides.
9. **Models and providers (revised): skipped.** No image generation change, no OpenRouter key, nothing decided on BYOK, model router or per-agent split. Those items stay open.
10. **Reply surfaces (revised):** thread titles via `assistant.threads.setTitle` in DMs and agent view only. Topic summaries become a runtime skill named `topic-summaries` (may set the topic or thread title). No thinking display. Marker enforcement closed (stepGuard plus prompt position).
11. **Scope cuts (revised): deferred, left open:** E2B repo templates, repo notes, skills import, fallback upload host, web UI / AgentController, sandbox-first, HuddleFM, log shipping. Legacy Gorkie auto-add: pending, owner asked what it is.
12. **Mechanical approvals:** yes to `member_joined_channel`/`member_left_channel` (done) and `CREDENTIALS_KEY_PREVIOUS` (done). The `@chat-adapter/slack` patch was dropped, then approved in the final review when native streaming was turned back on (924cbb0e).

### Open owner decisions (2026-09-24)

- [x] MCP approval in shared threads (owner 2026-09-24: "if the user wants full psycho mode on their mcp let them"). `mcp/user-servers/approval.ts` no longer calls `levelOutsideDM`: an MCP server's App Home level applies as set in every thread, including 'never' and 'delete' in shared threads (the clamp from e5588027 raised both to 'write' outside DMs). GitHub keeps the clamp (`lib/github/access.ts`, `app-home/github/blocks.ts`, README "never ask falls back to asking before writing"); the owner scoped this to MCP. Shared-thread MCP still needs the per-person opt-in.
- [x] Gate order item 1: chat commands skip the usage limit. Owner 2026-09-24: yes, keep. No code change; this is current behavior.
- [x] Gate order item 4: button clicks. Owner 2026-09-24: approval buttons keep the original gorkie behavior, only the person whose turn it is can answer. Already true, no code change: the `@mastra/core` patch refuses an approve or deny click unless the clicker matches the requester (in-memory stash, else the requester persisted with the suspended run; with neither, the click is refused). Bans still apply to every click; opt-in, focus and usage limits are not re-checked on clicks.
- [ ] App Home fails to publish when the GitHub installation count lookup throws. Needs an 'unavailable' state instead of failing the whole Home tab.
- [ ] Security review #3 residual (untraced): one review agent reported that a message sent by B while A's run is active executes with A's request context, and so with A's tools (core `dispatchInboundMessage` has no `ifActive`). The reviewer did not trace it. Verify before relying on the shared-thread opt-in.

## Owner checklists (2026-09-24)

Things only the owner can do: deploy setup, prod checks, and live tests in Slack. Detail lives in the linked items below.

### Deploy setup (current branch)

- [ ] Migrations `20260924054707_usage_turns` (usage limits) and the Mastra `mastra_background_tasks` store (created on start) run on deploy.
- [ ] Run `bun run db:migrate`: `20260924053636_github_dm_only_last_error` (drops the old shared-thread GitHub opt-in, adds `github_credentials.last_error`), `20260924062558_github_mcp_threads` (new per-user GitHub and MCP shared-thread opt-ins) and `20260924064740_mcp_error_http_status` (backfills `last_error_http_status`).
- [ ] Re-enable the shared-thread toggles: the DM-only migration dropped every GitHub opt-in, so anyone who used GitHub or MCP in shared threads turns it back on in App Home.
- [ ] Env: `PUBLIC_BASE_URL` (https in prod; `http://localhost:4111` for dev OAuth), `SLACK_SIGNING_SECRET`, `GORKIE_API_TOKEN` (required in prod, 32+ chars), `MODERATORS` (Slack user ids), `LOGS_CHANNEL` (the gorkie-logs channel id; invite the bot). Set `DATABASE_URL` to `sslmode=verify-full` instead of `sslmode=require`, which triggers a pg warning.
- [ ] Manifests: replace `<your-host>` / `<YOUR-TUNNEL-URL>` in `slack-manifest.json` and `slack-manifest.dev.json`, push both, reinstall the app (`/ban` and `/unban`, the `member_joined_channel`/`member_left_channel` events added in db97ea51, and the bot scopes trimmed in 64e0c361; the live-view `links.embed:write` scope, `unfurl_domains` and `link_shared` event were removed again in ea6ed97a).
- [ ] GitHub App: Callback URL `<PUBLIC_BASE_URL>/oauth/github/callback` (plus the localhost one for dev), Setup URL `<PUBLIC_BASE_URL>/oauth/github/installed` with Redirect on update. Turn device flow off after deploy.
- [ ] cloudflared ingress: only the Slack webhook, `/health` and `/oauth/*` (docs/webhook-mode.md). The `/live/*` and `/browser/orchestrator/stream` rules are gone with the live preview (ea6ed97a); remove them if they were added.
- [ ] `gorkie-monitor.sh` probes `/health`.
- [x] `bun run build:template` for `gorkie-workspace:2.2` (2026-09-24). Built in 1m45s after e6184083 dropped the root `ensure_binary()` step that segfaulted the builder (exit 139). Reported the same template id as 2.1 (tag on the same template). Not yet verified in a live sandbox that the user-level `python3 -m cloakbrowser install` step fetched the binary: open a thread, run `stealth-browser.sh`, confirm it launches.
- [ ] Database cutover: `pg_dump`, dry-run `drizzle/20260924024825_slack_memory_thread_ids` on a copy (runbook `docs/slack-thread-ids.md`, watch the time on large snapshot tables), then deploy with the bot stopped. Confirm the other migrations applied (`\d mcp_servers`, `\d user_settings`).
- [ ] Delete `src/mastra/public/observability.duckdb` and its `.wal` (2.4 GB, gitignored, but Mastra copies `public/` into the build output). Production creates no DuckDB.
- [ ] URGENT: check `ss -ltnp | grep 4111` on the prod host. Before this branch the Mastra API listened on all interfaces with no auth.

### Decisions and cleanup

- [ ] GitHub Support purge of 10594bb and 0dcae31 (the leaked zip; see the LEAK item): file it or decide not to.
- [ ] `CREDENTIALS_KEY` rotation (01eaff53): boot once on the new code first so every row is resealed as `v2`; then set `CREDENTIALS_KEY_PREVIOUS` to the old key and `CREDENTIALS_KEY` to the new one, restart, and remove `CREDENTIALS_KEY_PREVIOUS` after one clean start (README). MCP OAuth rows marked needs-auth because the previous key was missing need Reconnect.
- [ ] Secret rotation: decide on `SLACK_USER_TOKEN` and `CREDENTIALS_KEY` (both printed into a review agent's context; rotate `CREDENTIALS_KEY` only after WP-H lands) and the bot token.
- [ ] Remove `BUN_OPTIONS=--smol` from the systemd drop-in, then watch `memory.current` for a day (leak vs working set). Set systemd `TimeoutStopSec` >= 250s (drain: `server.drainTimeout` 120s plus the turn drain, a9a65778).
- [ ] Langfuse model price definitions for glm-5.3-flash, deepseek-v4-flash-vision-exp, gemini-3.5-flash-lite and mimo-v2.5 (cost shows 0 today).
- [ ] GitHub housekeeping: close issue 34 with the correction, close PR 13, triage the open issues.
- [ ] File upstream: `WorkspaceSkills` raw console logging, the `@mastra/core` patch hunks, and `AgentChannels.log` passing the string "CHANNEL" as log meta so pino spreads it character by character (cosmetic, `agent-DwtTO5Px.js:22131`).
- [ ] Emoji proxy delete endpoint (proxy repo), Gorkie emoji art, account move.

### Live tests (in Slack or prod)

- [ ] Tool display with native streaming back on (924cbb0e): Compact and Detailed render; Hidden mode, `!display`, and the App Home radio.
- [ ] Streaming recovery: a turn long enough for Slack to expire the stream still posts its whole reply (continues in a new message).
- [ ] Approval from a scheduled run: only the task's creator can approve; a card with no stored requester is refused.
- [ ] Shared-thread toggles: with GitHub and MCP shared threads on, tools load in a channel thread with ask-before-writing; with them off, the handoff message.
- [ ] OAuth single-use link: a Connect link works once; reusing it fails; the interstitial shows @handle and id.
- [ ] Default-branch refusal: on a repo whose default branch is `develop`, `github_push_branch` to `develop` is refused.
- [ ] `@gorkie !help` as a root mention answers without making gorkie follow the thread.
- [ ] A background job plus `!stop`: the job is killed and nothing wakes afterwards.
- [ ] Bans: `/ban`, `/unban`, expiry, the gorkie-logs card buttons (moderator and non-moderator), a banned user in a mention, DM, followed thread, approval click, App Home and a scheduled task.
- [ ] Scheduled task fires and replies (Priority 1); streaming on scheduled tools; recreate schedules made before 09-13.
- [ ] Two approvals in one turn; approver identity with two people in one thread.
- [ ] GitHub end to end, including the security properties: web sign-in, shared-thread handoff, shallow push retry, `safe.directory` on a resumed sandbox, 8h refresh.
- [ ] MCP end to end: add, OAuth connect, revoke (no retry loop), expiry refresh, disconnect revokes; prod `mcp_servers.token` rows all carry the encrypted prefix.
- [ ] Images end to end on every ladder rung; streaming upload in a channel, a thread, a DM and a large file.
- [ ] Thumbs up/down land on the right trace in the `production` environment; fallback escalation.
- [ ] Langfuse: a Slack thread id groups into one session, `userId`/`channelId` filter, dev and prod environments separate; `excludeSpanTypes` volume after a day.
- [ ] OM runaway-thread replay (the 2026-08-22 thread in `.issues/traces`); the two DM "stopped midway" reports vs the host journal.
- [ ] Backgrounding (98320648): the completion wake posts into the same thread; the woken run reaches the sandbox; a job longer than 16 minutes survives; background `execute_command` plus `wait` makes progress; `!stop` kills `run_background`. Confirm the E2B plan runtime limit allows 60-minute jobs.
- [ ] Commands and limits (89c87891): a DM or agent-view thread gets a title (`agents.sessions.rename`); `!compact` runs; usage limits trigger (temporarily lower `turnsPerHour`) and App Home shows what is left; the 200k output cap turns tools off.
- [ ] Sandbox parity with kyto/coolton on a compile task; a "finished but not posted" repro.
- [ ] Schedules and sandbox (671e3e3c): a wait fires and its row is deleted, also when the run errors or is stopped; the sandbox pauses at turn end for workspace-only and custom-tool-only turns; a long tool run keeps it alive.
- [ ] GitHub DMs only and tool search (ebfdbafeb2): in a DM, GitHub work searches for its tools first; a push approval resumes; the model re-searches after OM observes the loaded tools; revoking the App grant shows "needs reconnecting" in App Home; shared threads still get the handoff.
- [ ] `!stop` with a queued message: no reply to the queued message and no leftover placeholder.
- [ ] MCP backoff: Fathom records `(HTTP 401)` as its last error and is not retried until reconnected.
- [ ] WP-L (4070081f): `call_slack_api` `pins.list` works in a public channel, is refused in a private one, and rejects a `token` argument; focus in a channel thread (outsider ignored, owner and moderator pass, `!focus off`); a long research task returns an artifact id that `read_artifact` opens; `!stop` kills a background `sleep 600` and a `run_background` job.
- [ ] Restart drain: restart the service mid-turn; the turn finishes (or posts "that turn stopped") and approval-parked runs survive.
- [ ] Unthreaded root: delete a thread's root message mid-answer; no footer at channel root.
- [ ] Opt-in: leave and rejoin the opt-in channel; access is revoked and restored without a restart.
- [ ] Attach a png in Slack: the model sees it and is not told it is "not downloaded".
- [ ] Thumbs and `submit_feedback` reports land in Langfuse environment `production` (score name `report` for reports).
- [ ] Output guards from the Langfuse audit: narration-then-stop and fallback markup leak no longer reach Slack.

## Backgrounding

Ported 2026-09-24 (98320648); history in IMPLEMENTED.md. What is left:

- [ ] Step 5: prompt guidance for the model to prefer `run_background` for long jobs, only after the live checks in the owner checklist pass.
- [ ] A host restart marks running background tasks failed without calling `onFailed`, so nothing wakes the thread. Decide whether a boot-time sweep should notify those threads.

## Priority order (set 2026-09-13 by owner)

Ship in this order. Everything else waits behind these.
1. Fix and test scheduled tasks end to end. ROOT CAUSE FOUND + FIXED 2026-09-13: a fired task woke with no Slack channel context, so `workspace-instructions-processor` -> workspace sandbox resolver (`channelContext(requestContext).threadId`) threw "No thread id available for workspace", failing every fallback model. `create.ts` now passes `ifActive: persist` + `ifIdle: { wake, streamOptions: { requestContext } }` like `wait.ts` did. Still to do: live-verify a task fires and responds, and note EXISTING schedules stored before the fix lack the requestContext so must be recreated.
2. Multi-tool behaviour [sequential-execution half done 2026-09-24, 424cfdf3; the second-approval failure is still open]: the second tool approval in one turn failing (`AGENT_RESUME_TOOL_CALL_NOT_SUSPENDED`), and `github_push_branch` forcing sequential execution (drops concurrency 10 -> 1 for every step once GitHub is connected). Parallel/approval tool flow must be reliable.
3. [done 2026-09-24, ebfdbafeb2] New search: GitHub tools behind `search_tools` via `includeResolvedTools` (also fixes the `search_tools` `github_*` lie).
4. [done 2026-09-24, 7b79850e] Socket Mode -> webhook mode, then OAuth (MCP + GitHub) on the public ingress it unlocks.
5. [done 2026-09-24, 32060ee4] Ban support.
6. [done 2026-09-24, 89c87891] Usage limits (per-user rate limiting).
7. [done 2026-09-24, d5e2adf0, 446a195f, 924cbb0e] Agent security scan.
Also wanted, behind the above: focus mode (kyto-style context filtering), the browser embed preview (built 2026-09-24 in a04990e, then scrapped by the owner in ea6ed97a), and landing the backgrounding design (cap, keepalive and completion wake done 2026-09-24, 98320648; auto-background-on-timeout still open). Providers.ts cache cleanup is a quick win alongside.

## Audit before anything else

- [ ] **Live-verify the shallow-push retry on a private repo.** `push.ts` now catches `/shallow/i` and retries once after `git fetch --unshallow`. Confirm a `github_checkout` → commit → `github_push_branch` round trip works on both a public and a private repo, and that the unshallow fetch completes inside `sandbox.gitTimeout` (5 min). `--filter=blob:none` was considered and rejected: a treeless clone needs github.com auth for later blob fetches, which happen outside the credential window.
- [ ] **LEAK: `mastra-db9c3d0be7.zip` (private quiverai-monorepo `packages/mastra` source) was committed by `git add -A` in 0dcae31 and squash-merged to public `main` as 10594bb (PR #24).** Only placeholder values in its `.env.example`, no live secrets. Owner chose a history rewrite: both commits recreated with identical tree minus the zip and force-pushed with lease (`main` 10594bb -> 6bae9df, `github-setup` 0dcae31 -> ddc654d). Zip moved to `/workspaces/mastra-db9c3d0be7.zip`, `*.zip` added to `.gitignore`. STILL OPEN (owner): GitHub still serves 10594bb by SHA via PR #24 refs; file a GitHub Support request to purge cached views and dereference 10594bb and 0dcae31. Lesson: stage explicit paths, never `git add -A`, when the owner may drop files into the repo root.
- [ ] Optional: periodic rebuild of the opt-in allow-list. Membership events now keep it live (WP-D, db97ea51), but it sits in `MastraStateAdapter`'s in-process cache and is only rebuilt at boot, so a missed event persists until restart.
- [ ] **Production Langfuse audit 2026-09-24 [status 2026-09-24: fixed in tree, not yet deployed or live-verified: 1, 2, 3, 4, 5, 6, 7 (except pre-09-23 schedules stay sessionless, and resumed runs still overwrite trace input); already resolved in prod: 9; 10 and 12 fixed 2026-09-24 in 424cfdf3 (10: premise wrong, `external_read_file` returns display text, prompt now says parse with `node:fs/promises` readFile and use view_image; 12: logger.error + span error); 8 and 14 fixed 2026-09-24 in a9a65778 (8: `chat/turn-drain.ts` `TurnDrainWorker` waits for in-flight turns, skips approval-parked runs, aborts stragglers about 10s before the deadline, plus `server.drainTimeout` 120s prod / 10s dev in `index.ts`; rejected: drainTimeout alone (does not cover Slack turns), a raw SIGTERM handler, `Chat.shutdown()`; the implementer's `slack.disconnect()` call was removed by the lead because it is a no-op in webhook mode and the deployer's `server.close()` already refuses new webhook deliveries before workers drain (`@mastra/deployer` dist/server/index.js ~4776-4800); OPS: systemd `TimeoutStopSec` >= 250s and a live restart test. 14: `observability/model-errors.ts` ErrorProcessor registered FIRST in `lib/error-handling.ts`, since Mastra stops at the first processor that requests a retry; Langfuse cost 0 remains OPS, model prices); 11 fixed 2026-09-24 in 98320648 (live-job registry defers the pause, 10-min keepalive); STILL OPEN: 13 one-offs (upload_emoji 502 on existing name etc.)] (725 traces, 09-18..09-24, deployed core 1.66.0 / e2b 0.11.0). Still live in prod AND the tree unless noted.** Raw pulls in the session scratchpad `langfuse/` only (real user content; never commit). (1) HIGH: narration-then-stop, 14 runs: the mandatory marked narration line (`prompts/reasoning.ts:3`, `prompts/core.ts:30`) makes glm-5.3-flash end the step after the line with no tool call; nothing re-prompts (`stopWhen` only skip/wait). e.g. 2735998c3648b4691db37bea4f8cfd44, 9be9d62ead6d81b83bf009ec35696b40. Fix: processor that re-prompts once on a lone marker line, or move narration out of reply text. (2) HIGH: Hack Club fallback leaks raw `<arg_value>`/`</tool_call>` markup into Slack (6 of 16 turns on 09-21 13:13-13:38, tool never ran), e.g. 882114ff1a2ace4fbcf541875a5f142f; no output guard for model-internal markup; `preferLastWorking` (`providers.ts`, 30 min TTL) pinned the fallback though the primary recovered. Fix: strip/parse `<tool_call>`/`<arg_key>`/`<think>` before render; never pin a non-primary rung. (3) FIXED BY TREE: 'stay silent' turns posted filler (16 turns, e.g. 374cb5ca38794bea33d61da611402550): no `skip` tool in prod; restored in the port. (4) FIXED BY TREE: code-mode programs killed at 60s (21 calls, e.g. fb8e5e39eaee9a6e14b61db934692ede): @mastra/e2b 0.11.0 passes no process timeout; 0.12.1 passes the sandbox timeout. (5) MEDIUM: every second edit to a file rejected 'has not been read' (84 errors, 26 traces, e.g. baa762f8bb778c642289668e617379c8): read-before-write on (`workspace/index.ts` ~126) and Mastra clears the read record after each successful write; in-memory only. Fix: drop the requirement for edit_file (old_string already guards staleness). (6) MEDIUM: image generation down since 09-22 17:07, bare 'Not Found' (Hack Club proxy 404 on `google/gemini-3.1-flash-image`); model treats it as a refusal and retries, even via `wait`. Fix: repoint model id; map 404 to 'model unavailable, do not retry'. (7) MEDIUM: approval-resumed runs and scheduled tasks lose Slack sessionId/user in Langfuse (10 traces, e.g. 848ca48c1407fd32da7a3e2b49cd8425 with 9 approval clicks in 70s for read-only GitHub calls): `slackIdentity` stamps only root spans, resumes are child spans; trace output stuck at 'suspended'. (8) MEDIUM KNOWN (ISSUES.md G): restarts kill turns mid-flight (1eac8c3e16f9bf1bc965d44e4ef3d903, c07a105947f7f02346264156e19f671b, 425d74eb0ae5df41e737536feb0cdc29); no SIGTERM handling. (9) RESOLVED in prod 09-20 ~11:15: `invalid_action_token` on search (121 occurrences). (10) MEDIUM KNOWN (A4): code mode JSON.parse of external_read_file output (6 calls); prompt still recommends JSON exports. (11) MEDIUM KNOWN (Backgrounding, E3): background jobs reaped between wait turns; two wakes ended with no reply (dc97ae1eab14a5e9c8c7e255dfb01258). (12) LOW: code-mode failures logged at normal level, invisible to ERROR filters (57). (13) LOW: one-offs: upload_emoji proxy 502 on existing name, react on nonexistent emoji, Exa over capacity, fetch_url on JS pages, wrong model-supplied ids, view_image on .mp4. (14) LOW observability: no provider/fallback errors exported, summarizer traces have no session, cost 0 (no model prices in Langfuse). Latency median 17.7s, p99 910s.
  - [x] (1) and (2) fixed 2026-09-24, awaiting a live test. Root causes confirmed from the traces: 2735998c is one step, `finish_reasons: ["stop"]`, text only `→ _checking today's date..._`, no tool call; 882114ff on `z-ai/glm-5.3-flash` via Hack Club parsed one parallel call (`search_web`) and streamed the other (`get_channel_info`) as fragments (`_<arg_value>C0B4W8S1N3Z`, `Id</arg_key><arg_value>...</tool_call>`), so there is no tool name to rebuild a call from and parsing it back is impossible for the observed shape. New `processors/step-guard.ts` on orchestrator, research and explore (first output processor): `processOutputStream` strips `<think>`/`<annotation>` blocks and everything from a tool tag (`<tool_call>`, `<arg_key>`, `<arg_value>`, or a stray closer) to `</tool_call>` or step end, holding back a tag split across deltas; `processOutputResult` scrubs the persisted text parts the same way; `processOutputStep` calls `abort(reason, { retry: true })` when the step leaked tool markup or ended with no tool call and only marker lines. Native mechanism: `retry: true` rolls the step back out of the message list and adds `[Processor Feedback] ...` as a system message (`agent-DwtTO5Px.js:27578`), channels' `postTripwire` skips posting retry tripwires, and `maxProcessorRetries: 2` was already set on all three agents; the guard adds its own one-retry-per-turn cap in per-request processor state because Mastra resets `retryCount` after any non-retry step. `onIterationComplete({ continue: true })` was considered and rejected: its `feedback` is only injected when the step was already continuing, so a stalled final step would resume with no instruction, and a leaked step would stay in history. `extractReasoningMiddleware` not restored: the opencode rung is a router string, and the stream processor covers both routes. Note `processOutputStep` must return `messageList`, not `[]` as the Mastra docs example shows, since a returned array replaces the message list. Prompt: `reasoning.ts` and `core.ts` now say a marked line goes out with the tool call it announces and never ends a response; the 10-15 tool call check-in rule is unchanged. `preferLastWorking` pin: `rememberModel` now uses `setIfNotExists` so answers never renew the pin, and `config.workingModel.ttl` dropped from 30 to 5 min, so a failover pins the fallback for at most 5 minutes and the next turn re-probes the primary. Ladder unchanged.
- [ ] **Work through the whole backlog (asked 2026-09-24).** Owner: fix all open items from the Langfuse audit except #13 (one-off upstream/model failures), then go through every open TODO item, idea and broad feature and implement it. Approach: triage every open item (buildable now / needs owner decision / live test or ops only / already done or stale), group buildable ones into non-overlapping work packages, build in waves, commit and push each wave to chore/quality-pass.
- [x] **Slopradar pass (asked 2026-09-24).** [Status 2026-09-24: all phases done and logged in IMPLEMENTED.md: annotate, compile, resolve (82771c5b, 64e0c361, e6184083), verify (4df89a8d), /no-comments (39975326), desloppify last.] Adapted from the owner's slopradar skill (written for another repo) to gorkie: rules = AGENTS.md, CODING_STANDARDS.md, code-review-and-quality and code-simplification skills. Phase 1 annotate (reviewers add `TODO(slopradar)` comments per scope, no code changes), Phase 2 compile into `/SLOPRADAR.md` (score per scope, cross-scope patterns, rulebook contradictions, bands, orphans, keeps), Phase 3 resolve: owner decisions and rulebook contradictions go to TODO.md 'Owner decisions from slopradar' first; resolve the rest band by band, log each resolution in IMPLEMENTED.md; verifier agents refute; done when `grep -rn 'TODO(slopradar)' src scripts workspace docs` is empty and checks pass. Then /no-comments over the resolution diff and desloppify last.
- [ ] **Slopradar leftovers from verification (2026-09-24).** S3 partial: `run_background` is not blocked while a GitHub push/checkout holds the user's token, and a concurrent foreground command can use it too. S9 premise false: consumed OAuth start nonces live in `MastraStateAdapter`, in-process memory, so reusable after a restart and never expired. S8: `links:read` removed from both manifests 2026-09-24 (owner: drop it; needs a manifest push and reinstall); `is_mcp_enabled: true` unreviewed. `drizzle/20260924024825_slack_memory_thread_ids/migration.sql` never deletes orphaned thread-state rows (needs a new migration). Each turn reads `user_settings` 3 to 5 times. `github/checkout.ts` resolves repo access twice on an approved checkout. `tools/slack/get-slack-file.ts` lets two same-named files overwrite each other. Comments beyond vendor facts: `db/queries/usage.ts:64`, `mcp/oauth.ts:26,138`, `workspace/filesystem.ts:171`, `mcp/user-servers/tools.ts:9`. agentmail `references/api.md` label filter may miss inbound replies (unverified).
- [ ] **Remove the pre-rename thread state keys after 2026-10-24.** `focus` and `slackTitle` in `threadStateSchema` (`src/mastra/types/thread.ts`) are read so a stored focus list survives the 2026-09-24 rename; the next write drops them. After 2026-10-24 a thread idle since the rename only loses its focus list and a title dedupe, so delete both keys and their reads. The date used to live in a code comment that /no-comments removed.
- [ ] **No-comments leftovers (2026-09-24).** `unlabelledServers` (`mcp/user-servers/tools.ts`) is a Home warning kept only in process memory, lost on restart; persist it next to `setMCPServerError` or accept it as transient. `research`/`explore` must not get the `sandbox` output processor: `endSandboxTurn` keys on the thread, which a subagent shares with its parent, so a subagent would pause the parent's sandbox mid-turn. Only the orchestrator's processor ends a turn; encoding that (key by run) is open.
- [ ] **Owner decisions from slopradar (accepted 2026-09-24, all recommended defaults).** (1) comments: vendor/platform facts plus a short why on fire-and-forget promises and intentionally ignored catches; (2) closures over ~20 lines may be named functions, otherwise inline one-use helpers; shared/exported shapes in types/, single-consumer private shapes move back inline; (3) AGENTS.md carve-out for chat/history.ts (channels backfills only the first mention); (4) rewrite the CODING_STANDARDS Slack modal section for Chat SDK, drop the views.update hash rule; (5) document the drizzle.config.ts process.env exception; (6) a deployment-tunable value goes in config.ts even if used once; (7) keep the migration chain; (8) AgentMail: email bodies untrusted, send only when the requester explicitly asks this turn, drop webhooks/websockets references and org-admin API calls; (9) trim unused manifest scopes (reinstall), keep link_shared (required by unfurl_domains); (10) keep the v1 decrypt path and @vercel/connect for now; (11) research/explore get no persistent memory; (12) uncaught exception: log, drain, exit (systemd restarts); (13) summarize_thread drops ## side comments; (14) keep the resumable download; (15) keep the 5-minute fallback pin; (16) runtime skills follow unslop on dashes.
- [ ] **Sanity-check the shared-thread GitHub handoff.** Every `github_*` tool in a shared thread now returns the handoff message with `needsApproval: false` and no SDK formatter. Confirm `github_list_pull_request_files` in a shared thread returns the DM instruction rather than throwing `files.map is not a function`, and that dropping the approval prompt there reads right.

Three surfaces that are shipped, reachable by real people, and never verified end to end. Each needs a pass that proves it both *works* and *is safe*, not a spot check. Do these before new features.

- [ ] **Images, end to end.** Test every path an image can take and confirm the model actually sees it rather than describing what it expects: an image uploaded in Slack (channels inlines png, jpeg, webp and pdf as file parts before we see them), `read_file` on an image in the sandbox (now that `mediaTypes` includes `image/gif`), an image returned from a tool result (`moveToolImages` relocates `image/*` into a user message), and an image produced by `generate_image` then sent back with `upload_file`. Run each against both providers, opencode-go and Hack Club, since vision support is the open question. Known traps to check off while there: `read_file` with any `encoding` silently degrades an image to text; an image over `maxMediaBytes` (10 MB default) silently degrades to a metadata string; code mode's `external_read_file` JSON-stringifies its return so an image is flattened to base64 text; and `chat/attachments.ts` still tells the model files are "not downloaded yet" when four mime types are already inline.
- [ ] **GitHub, end to end, including the security properties.** 34 tools, a credential broker and a per-user permission model, none of it live-tested. Prove in order: the 34 appear for a connected person and none for a disconnected one; the three App Home presets gate the right calls (`all` 34/34, `write` 14/34, `delete` 2/34); a token refreshed mid-conversation is picked up per call; `github_push_branch` actually pushes and the credential is dropped afterwards on both the success and failure paths; a disconnect mid-turn surfaces as a tool error rather than a silent failure; the App Home web sign-in (`/oauth/github/start` through `/oauth/github/callback`) completes and the Home tab updates itself; the 8 hour refresh works and a failed refresh disconnects cleanly. Two security properties specifically, because both are currently only argued rather than observed: the approver-identity patch must ignore a click from anyone other than the person asked, and a `github_checkout` clone must not leave a private repository readable by everyone else in a shared thread's sandbox.
- [ ] **MCP servers, end to end, including the security properties.** Per-user servers with encrypted bearer tokens, and the owner believes almost nobody has one configured, which means bugs here have been invisible rather than absent. Prove: a server can be added, connects, and its tools appear only for that user; a bad URL or token surfaces in App Home instead of retrying forever (the Fathom `invalid_token` loop is the known counterexample); the plaintext-to-encrypted migration handles existing `mcp_servers.token` rows that have no `v1.` prefix; `CREDENTIALS_KEY` is documented as unrotatable before anyone tries; and the approval gate actually holds, noting it currently matches tool names against `^(delete|remove)_`, which is a naming convention rather than a capability check and will miss a destructive tool called anything else.
- [ ] Two more "this thread stopped midway" reports from issue 35 to line up against the diagnosis, both DMs so neither is in the downloaded corpus (corpus is channel threads). `hackclub.slack.com/archives/D0BGJGU5108/p1789170614666169` (posted ~8h before 2026-09-12) and the issue's original `2e7ec2bacc767b725194d64ed0512ffe`. The G-section diagnosis says these leave no trace because the root span never completes, so the way to confirm is host-side: match the timestamp against the journal for a watchdog restart or an OOM, not against Langfuse. If either falls after the 2026-09-12 watchdog rewrite and memory bump, it is a new instance and the diagnosis is incomplete.
- [ ] **Compaction / observational memory in a multi-user thread, tested against the thread that already broke.** The runaway is in the corpus: `slack:C0A6C5F52BE:1787322260.792609` on 2026-08-22 produced three back-to-back traces of 237 MB, 177 MB and 228 MB between 04:27 and 06:19 UTC, then another 155 MB at 07:08, sixteen traces and about 0.8 GB from one thread in a day. Trace ids, oldest first: `d555dbc5...` (2026-08-21 14:24), `de4fe5d9...`, `3b28f2cc...`, `0de0c295...`, then the three giants `b159ed7f...` (04:27, 237 MB), `b3be47c8...` (05:41, 177 MB), `06647200...` (06:19, 228 MB), then `82527692...` (07:08, 155 MB) and the tail `a90928d3, c3669722, 68f9423d, 3cff081d, e0216f5d, f4a3e83d, d4b7394d, f37a897f`. All under `.issues/traces/<id>.json`. This is the concrete regression to replay once compaction is touched: read the giants to see what the context grew into, confirm whether it was history that never compacted or the same input re-serialized per span (the 95 MB-trace entry found the latter elsewhere), and only call OM's multi-user behaviour correct when this thread stays bounded. Do not test compaction on synthetic input while this real failure sits unread.
- [ ] **Background sandbox commands, tested for real.** Now that `execute_command` accepts `background: true`, the abort signal is `false` so processes outlive the turn, and the VM lifetime exceeds the execution cap, prove the three things that were broken independently: a build longer than the old 8 minutes finishes instead of the VM being killed under it; a `background: true` dev server is still running and reachable on the next turn (a paused-then-resumed VM keeps it, per E2B's `stop()` doc); and `get_process_output` and `kill_process` actually reach the process the model started. The live transcript on 2026-09-12 (the LSPromise/IonStack APK build) is a natural test: it set up 6 GB of swap on its own and started a Gradle build, which is exactly the long-build case the 8 minute ceiling used to kill.

## Simplification

- [ ] **[Deferred, Q11 2026-09-24: left open.]** Consider `@mastra/e2b` 0.11.0 for sha-tagged repo templates. We are on 0.10.0-alpha.3, where the machinery exists but is unexported; 0.11.0 exports `createRepoTemplate`, `refreshRepoTemplate`, `repoTemplateRef` and `RepositoryAccess` from the package root. It bakes the clone at a commit plus the dependency install into the image, so a session does `git fetch` and a checkout instead of a cold clone and full install. One template per (repo, setup command, repoDir) with the commit sha as a docker tag rather than part of the name, which is what keeps warm reuse alive across commits. A moved head boots from the stable tag and rebuilds in the background, so only the first build ever blocks a start. Dependency change, so ask before doing it, and note `github_checkout` currently clones cold every time.

## Customization

Nothing open.

## Known issues

- [ ] **[Q3 decided 2026-09-24: keep open egress, add `denyOut` for RFC1918, link-local and metadata ranges in `workspace/network.ts`.]** Decide whether the sandbox should have unrestricted egress. `workspace/network.ts:6-20` returns only the AgentMail transform and never sets `allowOut` or `denyOut`, and per `e2b/dist/index.d.ts:8313` an unset `allowOut` allows all outbound traffic. That is the exfiltration leg for every item above: anything reaching the sandbox, a private-channel file, a checked-out private repo, a canvas, can be POSTed anywhere by agent-run code with no approval. If the intent is a locked-down sandbox this wants `allowOut` plus an explicit package and registry allowlist. Worth deciding deliberately rather than by default, since it is also the thing that makes the sandbox useful.
- [ ] Confirm `excludeSpanTypes` actually cut what it was projected to. Measured projection was 80.4% of spans dropped, about 29,000 units a month against the 50,000 free tier. Check the real number in Langfuse after a day rather than trusting the projection, and watch whether losing `processor_run` makes any real debugging harder before this becomes permanent.
- [ ] Decide whether the memory growth is a leak or a working set, now that the host has 2971MB and limits are `MemoryHigh=2000M` / `MemoryMax=2400M`. The old process went from about 600MB to 1.47GB over 15.5 hours with no observed plateau, roughly 50MB an hour. If it still climbs steadily after `excludeSpanTypes` lands then more RAM only changes the interval between OOM kills, and it needs a heap snapshot. If it rises and flattens, the extra memory was the whole fix. Watch `/sys/fs/cgroup/system.slice/gorkie.service/memory.current` over a day.
- [ ] Remove `BUN_OPTIONS=--smol` from `/etc/systemd/system/gorkie.service.d/smol.conf`. It puts Bun in reduced-memory mode, which was right on a 2GB host and now costs throughput for no reason.
- [ ] Verify the streaming file upload against real Slack. The rewrite in `src/mastra/tools/slack/upload-file.ts` streams from the sandbox to Slack's `getUploadURLExternal` endpoint and raises the cap to Slack's own 1GB. It was proved under Node in isolation, including that `duplex: 'half'` is required and that its absence is what broke every upload, but it has never run through the real Slack path. Test a channel, a thread, a DM, and something large.

- [ ] Test image handling properly end to end against both providers, opencode-go and Hack Club. Steering is confirmed working, so this is the remaining unknown in that area. Three things make it worth a deliberate pass rather than an incidental one: `preferLastWorking` can pin a turn to whichever model last answered for 30 minutes, so a text-only route can silently own the next image turn; nothing checks vision capability before sending a file part; and `provider-registry.json` stores the opencode-go models as a bare name list with no capability metadata, so it cannot be answered statically. Use `bun run traces --errors` afterwards to confirm which model actually served each attempt.
- [ ] A second tool approval inside one turn fails with `resumeStream() cannot resume tool call "<id>" because it is not suspended` (`AGENT_RESUME_TOOL_CALL_NOT_SUSPENDED`, `#validateSuspendedToolCallTarget`). Observed 2026-08-25 07:50 UTC on `1.62.0-alpha.9`: the first `github_push_branch` approval resumed fine and threw our own main-branch refusal at 07:46, the second was approved at 07:50 and the run could not find its suspension after the 2 second snapshot poll. The push never ran, confirmed against the API (`gorkie-agent/ai` untouched since 2026-08-07). Only occurrence in the error history back to 2026-08-23, so it arrived with the alpha, though a single sample is not proof. Our patch is not implicated: hunks 7 and 8 sit in the api-error retry path, and every `_buildRenderContext` call site matches the rebased arity. Needs a clean repro (two approvals in one turn, no other tools) before filing upstream.

- [ ] Confirm `git config --global --add safe.directory` is genuinely unnecessary on E2B. eve sets it for `/workspace` and the checkout because Vercel Sandbox snapshots are owned by a builder uid that differs from the session uid. E2B appears to run as one user throughout, and the live clone and push probes both worked without it, but that was a fresh sandbox rather than a resumed one. Worth checking against a paused-and-resumed sandbox before relying on it.

- [ ] Live-test the approver-identity patch: two people in one thread, gorkie asks A for approval, B clicks Approve. B's click must be ignored and A's must still work. Note B currently gets silence, not a message, since Mastra's handler has no ephemeral path in scope; decide whether that is acceptable or worth widening the patch.

- [ ] Live-test the Slack output changes: click a thumbs up and a thumbs down on a turn footer and confirm both record against the right trace, since the click payload shape was never confirmed against a real click. Same turn also exercises the footer's elapsed time and the suggested prompts now coming from adapter config.
- [ ] Live-test the `1.61.0` upgrade: reproduce a Luna rate-limit in Slack and confirm the fallback escalates silently, with a real error still surfacing once both models are exhausted. Extra reason to retest on 1.61: it refactored the response-message-id rotation (`rotateResponseMessageId` now seals the prior message via `messageList`) that the fallback-escalation hunk depends on.
- [ ] Drop the hand-rolled `workspace/filesystem.ts` once `@mastra/e2b` ships a filesystem provider (`mastra-ai/mastra#21875`, open).
- [ ] A turn can finish generating cleanly (`finishReason: 'stop'`, real usage stats) but the reply never reaches Slack. Likely the streaming/posting pipeline (`chat-channel-render` output processor or the Slack post call itself) failing silently after generation succeeds, not a model/provider issue. Needs a concrete repro thread to trace further.
- [ ] Streaming breaks on scheduled task tools. Likely the same root cause as the scheduled-tasks failure fixed 2026-09-13 (woken turn had no channel context -> workspace sandbox resolver threw -> models failed mid-run, surfacing as a streaming break). Re-verify after the `create.ts` ifIdle.requestContext fix; if a fresh scheduled task now streams cleanly, close this.
- [ ] (2026-09-24: the 1.66.0 patch is deleted but replaced by `@mastra+core@1.69.0.patch`, now 5 hunks incl. the fail-closed approval fallback; none are upstream yet, so still pinned) Get off `patches/@mastra+core@1.66.0.patch` and delete it. Asked for more than once and never written down, so it has been silently pinning us to one Mastra version: `package.json` maps `@mastra/core@1.66.0` to it exactly, which means every upgrade breaks the patch and has to be re-derived by hand against two built bundles (`dist/agent-CEHR0Wd8.js` and the matching `.cjs`), not source. Fourteen hunks carrying at least three distinct fixes worth upstreaming separately: a `step-finish` guard so a continued step (retry, in-place or fallback-model escalation) does not close the Slack session and finalize the message early; an approver-identity check so a tool approval from someone other than the person asked is ignored, which is what the open live-test item in Known issues exists to verify; and threading `message.author?.userId` into `_buildRenderContext` so that check has a requester to compare against. Read the whole patch before filing, those three were read off the first 40 lines.
- [ ] A 2.4 GB `observability.duckdb` is living inside `.mastra/output`, the directory that gets deployed. This is the same bug as the 2026-08-16 regression already in `IMPLEMENTED.md`, surviving in a new place rather than a fresh one. `src/mastra/index.ts:48` builds the path as `join(env.PROJECT_ROOT, 'observability.duckdb')` and `env.PROJECT_ROOT` defaults to `process.cwd()` (`src/env.ts:17`), so anything started the way the CLI suggests, `node .mastra/output/index.mjs`, has its cwd inside the output directory and accumulates the trace database there. The repo-root copy is 90 MB; the output copy reached 2,386,571,264 bytes with the disk at 85% full. The `PROJECT_ROOT` default cannot simply change, because `src/mastra/db/index.ts:10` resolves `drizzle/` the same way and `scripts/postbuild.ts` copies that folder into the output deliberately, so cwd-relative is right for the artifact and wrong only for this one file. The exporter is gated as `isProduction ? [] : [new MastraStorageExporter()]`, so confirm this is a local-start artifact rather than something production writes before choosing between pinning the DuckDB path explicitly and excluding it in postbuild. Delete the 2.4 GB file either way.

- [ ] ISSUES.md A1/D1 are still open: `read_file` has no `maxMediaBytes`/`maxOutputTokens` cap, `view_image` allows 10 MiB, and `moveToolImages` runs after the token limiter, so relocated images are not counted by it.

## Architecture

Six deepening candidates from the 2026-08-26 architecture review. All six agreed; work in this order.

## Ideas
- [ ] Focus mode gaps (focus shipped in 4070081f): already-stored memory from outsiders is not purged when focus turns on; `search_slack` can surface outsiders' messages; Mastra's approve/deny buttons are not focus-gated; outsiders are not counted in the backfill truncation note.

- [ ] **[Q9 skipped by owner 2026-09-24: left open, not decided.]** Steal eve's per-agent model split (`agent/lib/models.ts`). It gives the station that writes code the strongest coding model and deliberately runs the reviewer on a different vendor so the review is independent of the model that wrote the code. gorkie routes every agent through the same fallback ladder, so `explore` reviewing `orchestrator`'s work is the same model checking itself.
- [ ] Consider eve's `compaction: { thresholdPercent: 0.75 }` for gorkie. The per-turn 200k output cap is done (`processors/output-budget.ts`, 89c87891, orchestrator only); the session-level compaction threshold is not.
- [ ] **[Deferred, Q11 2026-09-24 (schema change): left open.]** Consider a repo-scoped notes document, eve's factory brain (`agent/lib/factory-brain.ts`): durable curated facts about a repository, capped at 40k chars, keyed by a hash of the repo and loaded at the start of work on it. gorkie's observational memory is thread-scoped, so what it learns about a repository in one thread is unavailable in the next.

- [ ] Live-test the GitHub swap end to end. Nothing below has run against Slack or a real repo: that the 34 tools appear for a connected person and none for a disconnected one; that App Home's three presets still gate the right calls (`all` 34/34, `write` 14/34, `delete` 2/34); that a token refreshed mid-conversation is picked up per call; that `github_push_branch` actually pushes and that the credential is dropped afterwards; and that a disconnect mid-turn surfaces as a tool error rather than a silent failure.

- [ ] **[Deferred, Q11 2026-09-24: left open.]** Skills support: let users add skills from skills.sh, plus custom skills (upload a ZIP, or import from a GitHub gist). Exploratory, not yet scoped.
- [ ] Mastra's `WorkspaceSkills` logs through raw `console.warn`/`console.error` instead of the configured logger, so its warnings bypass pino. Worth filing upstream; the local warning goes away once taste-skill is split.

## Product

- [ ] Issue 35, sandbox parity with kyto and coolton. Their sandboxes compile apps while gorkie's freeze. Compare the two setups rather than guessing, the reporter says theirs works on the same class of task.
- [ ] **[Q7 2026-09-24: build only after the completion wake is live-verified.]** Auto-convert a foreground `execute_command` that hits its timeout into a background job (Claude Code pattern: "moved to the background (ID ...), output is being written to ..."), then notify on completion. Backgrounding itself is ported (98320648: `run_background`, completion wake, live-job keepalive, required timeouts); Mastra has no native auto-convert. Note `defaultDisposition` now exists on 1.69 and is unused.
- [ ] Issue 35, scheduled tasks are reported broken. Confirm and fix. Related to the streaming-breaks-on-scheduled-task-tools item in this file. Owner reconfirmed broken 2026-09-13 and wants it fixed. Two known symptoms to reproduce with a dummy task (min interval 30 min, so `every 30 minutes`): (1) `Streaming breaks on scheduled task tools` (separate item in Known issues), and (2) verify a created task actually renders in the App Home Scheduled Tasks section now that its empty state was fixed. A dummy task must be made through the live bot (ask gorkie in Slack), not from a dev shell: creation goes through `context.mastra.schedules.create` on the running instance (`tools/scheduled-tasks/create.ts:53`), and importing the app to script it would start a second instance on the same Mastra scheduler and workers, so scheduled tasks could fire twice. Trace a scheduled-task fire end to end: the wake path is `schedules.create({ ifIdle: { behavior: 'wake' }, ifActive: { behavior: 'persist' } })`, so confirm the fire wakes the thread and the tool turn streams without the break.
- [ ] **[Deferred, Q11 2026-09-24: left open. Note it needs a deploy credential, which must not enter the sandbox.]** Issue 35, add a fallback file upload path. If Slack upload fails, upload through a temporary worker page. Put it in the skill or, better, the prompt, and add a worker skill.

- [ ] **[Pending, Q11 2026-09-24: owner asked what Legacy Gorkie auto-add is; answer before deciding.]** When someone adds gorkie to a channel, auto-add Legacy Gorkie alongside it, so the two stay in the same places without anyone tracking it by hand. There is already an `onMemberJoinedChannel` listener (`lib/allowed-users.ts` keeps the allowlist live through one, and `chat/events.ts` registers the greeting through another; the handler list is an array so a third coexists fine). The open part is what identity does the inviting: gorkie's own bot token cannot invite another app, so this likely needs the Legacy app to watch for the same event itself, or a user token.

- [ ] Rotate the bot token.
- [ ] **[Q9 skipped by owner 2026-09-24: left open.]** Add a model router.
- [ ] Let the `topic-summaries` skill set the channel topic itself: needs a `conversations.setTopic` tool, the `channels:write.topic` scope and a reinstall. The skill already exists (f60867a1) and currently tells the person to use `/topic`.
- [ ] **[Pending, Q11 2026-09-24: owner asked what this is; duplicate of the Legacy Gorkie auto-add item above.]** Auto-add Legacy Gorkie when Gork or Gorkie joins a channel, so joined channels stay tracked.
- [ ] **[Q9 skipped by owner 2026-09-24: no image generation change, left open.]** Image generation that does not depend on HCAI.
- [ ] Add a Slack emoji delete operation reachable from the search tool, matching the existing create and upload behaviour, with tests, then open a PR. Emoji creations are logged in the proxy channel as a requested upload followed by a successful creation.
- [ ] Custom Gorkie emojis based on the logo, covering different moods and tasks.
- [ ] **[Deferred, Q11 2026-09-24: left open.]** Look into Mastra AgentController plus web integration, and decide whether gorkie web is a thing we want.
- [ ] **[Deferred, Q11 2026-09-24: left open.]** Gork and HuddleFM integration.

## Rewrite parity

The Mastra rewrite absorbed sandboxes, skills, MCP, Slack support and steering. These are the
pieces that have not come back yet.

- [ ] Let gorkie edit or delete existing emojis. Upload and alias already work (`tools/upload-emoji.ts`); edit/delete waits on the emoji proxy delete endpoint (see the Product item on emoji delete).
- [ ] **[Q9 skipped by owner 2026-09-24: left open.]** Custom providers (BYOK): model and provider choice from models.dev, a $1 per day key usage cap, encrypted at rest. HCAI BYOK is not allowed. Open question worth settling before building: whether people would trust us to hold their keys at all, since the liability may not be worth it.

## Guardrails

Nothing open.

## Legacy backlog

- [ ] **[Q9 skipped by owner 2026-09-24: left open.]** Custom providers via a custom HCAI key, with model choice from models.dev and a $1 per day cap.
- [ ] **[Deferred, Q11 2026-09-24: left open.]** Consider making gorkie sandbox-first.
- [ ] **[Deferred, Q11 2026-09-24: left open.]** Log shipping (Logtail or Axiom) alongside Langfuse. The Langfuse half is done (see IMPLEMENTED.md).
- [ ] Move from TWA's account to Devarsh's account.
- [ ] Run the thermo-nuclear code quality review from `cursor/plugins`.
