# Template TODO

Only outstanding work lives here. Finished items move to
[IMPLEMENTED.md](./IMPLEMENTED.md) with their detail intact.

The sections above `## Product` are the reusable template: keep those limited to work that
belongs in anything built from this repo. Everything from `## Product` down is the gorkie
product backlog and is not expected to survive a fork.

## Simplification

- [ ] Drop the stranded `tool_display` column now that migrations can express it. It is still present in any database that ran the reverted tool-display work, and this is the first thing the new setup can actually fix: `drizzle-kit generate` will emit the `ALTER TABLE ... DROP COLUMN` once the column is gone from `schema.ts`, which it already is. Confirm no deployment still reads it first.
- [ ] Keep pruning one-use helpers and module-level Maps. Two passes done 2026-09-11. First: `readOnlyHintOf` inlined, and the `annotationCoverage` Map plus a local `counts` Map plus the `{annotated, total}` shape all collapsed into one `unlabelledServers` Set, because the only consumer was asking a yes/no question and the count appeared solely inside a warning string; that also removed an O(tools x servers) `find` inside a loop. `workspace/env.ts` deleted, its `sandboxEnv()` inlined into the one `E2BSandbox` construction. Second pass, after the owner overruled two judgement calls I had argued to keep: `sameModel` and `addServerModal` are now inlined too, along with `isGitHubUrl` (whose try/catch was dead, since `mcpServerSchema.url` is `z.url()` and already parsed at the call site), `GITHUB_SERVER_NAME` and `GITHUB_SETTINGS_URL`. `lib/github/urls.ts` is down to the single env-derived `GITHUB_INSTALL_URL`, which has two consumers and by the standards belongs in `config.ts`. Worth knowing for the next pass: a scan finds 114 exported symbols with exactly one external call site, but the large majority are module products rather than helpers, tools registered once in a toolset, prompts, `registerX` handlers, agents. Inlining those would be wrong. The rule to apply is small helper wrapping an expression, not one reference.
- [ ] Move `GITHUB_INSTALL_URL` from `lib/github/urls.ts` to `config.ts` and delete the file. It is the last thing in it, and it is a deployment-varying value built from `env.GITHUB_APP_SLUG`, which is where the standards put it.
- [ ] Move `github/views.ts` onto the `chat` builders once `dispatch_action` exists upstream. Filed 2026-09-11 as vercel/chat#926. Correcting an earlier estimate in this file: there is almost nothing to convert today. `failedModal` and `connectedModal` already use `Modal()` and `CardText()`, and the only two raw `ModalView` builders left, `configureView` and `connectView`, are exactly the two that need `dispatch_action` to re-render on change. So the "do the unblocked half now" plan was based on conflating hand-built lines with convertible ones, and the real answer is that the whole remaining conversion is blocked on that one feature. Decided against patching `chat` and `@chat-adapter/slack` for it: the change is small (two one-line runtime edits, `RadioSelect` whitelists the fields it forwards so an extra property is dropped, and the adapter's radio render omits `dispatch_action`), but every Mastra hunk we carry fixes something with no workaround, whereas this one has a working workaround already, and the version bump earlier today showed what a detached patch costs.
- [ ] Decide whether to drop the enumerated GitHub tool surface for the `gh` CLI, the way Mastra's own Factory does. Read from a sparse clone of `mastra-ai/mastra` on 2026-09-11 and spot-verified: it does not depend on `@github-tools/sdk` at all, has no allowlist, no preset, and no read/write classification of GitHub operations. Its entire GitHub tool registration is four tools in `mastracode/factory/src/integrations/github/session-subscriptions.ts:221-263`, each existing only because the CLI cannot do that job: refresh the credential, write a marker-guarded idempotent comment, subscribe and unsubscribe a PR. Everything else is `gh issue view`, `gh pr diff`, `gh api graphql` written as shell in skill markdown. The authorization boundary is the token's own GitHub-side permissions, not a name list, so adding a capability is zero code and the list cannot drift. Read/write separation is by module (`supervisor/read-tools.ts` has no `requireApproval`, `write-tools.ts` sets it on all seven), so the file boundary is the classification. The cost is the thing to weigh: this only works because a session-long `GH_TOKEN` lives in the sandbox env, which is a real widening of our current position, where a credential reaches the sandbox only for the duration of one brokered push.
- [ ] Steal Factory's four git credential lifetimes rather than its tool design, since these are portable even if the `gh` CLI question above is answered no. One, clone puts the token in the remote URL then resets it, and a leftover from a previous failed scrub is detected on every start with `if (/\/\/[^/]*@/.test(existing))` (`integrations/github/sandbox.ts:292-294`). Two, steady-state git auth is `credential.helper = '!gh auth git-credential'` (`:370`), which stores nothing on disk because git asks `gh`, which answers from the env. Three, `withInstallToken` (`:702`) is our `github_push_branch` broker as a named reusable primitive, scrubbing on both the success and failure paths. Four, the E2B template build passes `$GH_TOKEN` by reference, expanded in the build shell, so the stored layer command holds the variable name and never the value (`workspaces/e2b/src/utils/repo-template.ts:388-396`).
- [ ] Make the credential drop in `withCredential` (`tools/github/git-remote.ts:113-119`) strict, the way Factory's `scrubRemote` is. Ours logs and swallows: `.catch((error) => logger.error('[github] failed to drop the credential'))`. If `updateNetwork` fails to restore `baseRules()`, the firewall keeps injecting the `Authorization` header for that sandbox, so anything running in it can reach github.com authenticated until the sandbox dies, and nobody is told. Factory's equivalent throws on a failed scrub and appends it to the primary error rather than replacing it (`mastracode/factory/src/integrations/github/sandbox.ts:545-574`, plus `scrubbedFailure` at `:576-601`), so the caller keeps the error it would have had and the leak warning rides along. Worth copying that two-path shape: best-effort when the credential never got installed, strict once it did.
- [ ] Consider `@mastra/e2b` 0.11.0 for sha-tagged repo templates. We are on 0.10.0-alpha.3, where the machinery exists but is unexported; 0.11.0 exports `createRepoTemplate`, `refreshRepoTemplate`, `repoTemplateRef` and `RepositoryAccess` from the package root. It bakes the clone at a commit plus the dependency install into the image, so a session does `git fetch` and a checkout instead of a cold clone and full install. One template per (repo, setup command, repoDir) with the commit sha as a docker tag rather than part of the name, which is what keeps warm reuse alive across commits. A moved head boots from the stable tag and rebuilds in the background, so only the first build ever blocks a start. Dependency change, so ask before doing it, and note `github_checkout` currently clones cold every time.
- [ ] Split `workspace/skills/taste-skill/SKILL.md` (~16.6k tokens, well over Mastra's <5k-token recommendation) into `references/`.

## Customization

- [ ] Decide whether to let a person choose how tool calls are displayed, from App Home: `Hidden` (tools run behind the typing status) or `Cards` (Mastra's own `'grouped'` mode, one plan widget per turn). Built and then reverted on request, so the tab is back to the static `toolDisplay: 'hidden'`. What was built, if it is wanted again: both are native string modes, so there is no custom renderer and no patch, because `AgentChannels` keeps the adapter config by reference (`adapterConfigs[name] = cfg`) and reads `adapterConfig.toolDisplay` once per run, so a getter on it resolves inside the turn and follows whoever is being answered, fed by an `AsyncLocalStorage` set in `runTurn`. It needed a `tool_display` column, `chat/tool-display.ts`, `types/display.ts` and `app-home/tool-display/`. Never live-tested. The two items below only matter if this comes back. The owner also wants more than the two native modes: a status line, or kyto-style blocks, which means patching Mastra since neither is a native `toolDisplay` value. Decide that before rebuilding, because it changes the answer from a config setting to a custom renderer, which is the thing the reverted version deliberately avoided.
- [ ] Decide whether native `'grouped'` says enough. It never shows a result: `output` is set only for `'timeline'` (`agent-mTyOZvpk.js:19575`), so a finished task is a title and nothing else, and only a failure gets a body (`"⚠ " + errorText`, JSON truncated at 300 chars). Titles are `stripToolPrefix(toolName)` plus the first argument truncated to 35 chars, the plan header is rewritten to the latest tool on every call and result, and a delegated agent's steps arrive as their own top-level tasks. The template answered this with a custom `ToolDisplayFn` plus a patch pinning function-form display to `'grouped'`; that was built and then dropped in favour of staying native.
- [ ] Find out whether Slack caps the tasks in one plan widget, and fix the crash it causes. Investigated 2026-09-11 and verified against the installed source. Nothing between `pushToSession` (`@mastra/core/dist/agent-mTyOZvpk.js:19270-19279`) and `chat.appendStream` caps, chunks or counts tasks, so any limit is Slack's and can only arrive as a rejected append. The graceful-degrade path this file previously described is real (`@chat-adapter/slack/dist/index.js:4090-4112`) but it is not the whole story, which is why this reads as a crash rather than a downgrade: `fallback.nativeRendered` is set true by the first successful structured chunk, and at `:4081-4084` a later failing text flush then rethrows instead of falling back. So the first rejection is swallowed, and the next text flush throws out of `stream()`. That rethrow is worth fixing independently of any cap. On the number itself, 50 is observational: the only documented 50 in the installed surface is the 50-block message cap (`@slack/types/dist/chunk.d.ts:16`), not a task count, and `PlanBlock.tasks` declares no maximum. Plausible that N task cards expand to N blocks and hit the block ceiling, but confirm empirically at 45/50/55/60 distinct task ids and capture the platform error code before capping, since a guessed cap truncates real output. Note this is only reachable in `grouped` or `timeline`; with the committed `toolDisplay: 'hidden'` we emit no task chunks at all, so it rides on the reverted tool-display work above.
- [ ] Decide whether to enable resource-scoped Working Memory alongside the thread-scoped Observational Memory already in use, or leave it disabled.
- [ ] Add a concise plain-English response skill.
- [ ] Decide what to do about the `agent/skills/` tree that `npx skills add` writes at the repo root alongside `.agents/skills/`. Not a duplicate: it is the same skills with `name` and `disable-model-invocation` stripped from the frontmatter, a variant for agents that do not support those fields. Nothing in this repo reads it, since Claude Code goes through `.claude/skills` to `.agents/skills`, and dropping `disable-model-invocation` there would let these auto-trigger if anything ever did. Either delete it and gitignore it, or keep it on purpose. One file in it is already staged.

## Known issues

- [ ] A second tool approval inside one turn fails with `resumeStream() cannot resume tool call "<id>" because it is not suspended` (`AGENT_RESUME_TOOL_CALL_NOT_SUSPENDED`, `#validateSuspendedToolCallTarget`). Observed 2026-08-25 07:50 UTC on `1.62.0-alpha.9`: the first `github_push_branch` approval resumed fine and threw our own main-branch refusal at 07:46, the second was approved at 07:50 and the run could not find its suspension after the 2 second snapshot poll. The push never ran, confirmed against the API (`gorkie-agent/ai` untouched since 2026-08-07). Only occurrence in the error history back to 2026-08-23, so it arrived with the alpha, though a single sample is not proof. Our patch is not implicated: hunks 7 and 8 sit in the api-error retry path, and every `_buildRenderContext` call site matches the rebased arity. Needs a clean repro (two approvals in one turn, no other tools) before filing upstream.
- [ ] Collapse the GitHub prompt down to one flow. The surface now branches on credential kind (`app` or `pat`), on location (DM, shared thread, shared thread with `github_threads` on), and on approval preset, and the prompt carries a paragraph for most combinations. The combinations are not equivalent either: `github_fork_repository` works with a classic token and is structurally impossible through the App outside the repositories it is installed on, so the model is told about a tool that cannot work for half the people who see it. Decide on one supported path and write the prompt for that, rather than describing every branch.
- [ ] `search_tools` never returns `github_*`, so gorkie repeatedly told people the tools were unavailable and then called them successfully in the same thread. The tools come from the dynamic `tools` function while `ToolSearchProcessor` only indexes the static `deferredTools`, so they are always loaded and never findable. Same root cause as the `includeResolvedTools` item under Ideas. Until that is resolved, the prompt should tell the model that `github_*` tools are always present when GitHub is connected and never need searching for.

- [ ] A commit that only exists in the sandbox is not durable. `github_push_branch` is a separate turn from the commit, and the sandbox is paused at turn end and can be collected, so 587d0c5 (67ify footer fix, checks passed, diff clean) was lost between turns and had to be recreated. eve pushes in the same step that commits.
- [ ] `retryOnDead` cannot recover a `SandboxNotReadyError` (noted under Related on mastra-ai/mastra#22197, not yet split out). `get e2b()` (`:730`) throws `SandboxNotReadyError` ("Sandbox is not ready: <id>") whenever `_sandbox` is unset, and that matches no branch of `isSandboxDeadError`, patched or not. So the retry helps a sandbox that died remotely but not one that was never started; only calling `ensureRunning()` before touching `.e2b` covers that, and every caller has to know the distinction.

- [ ] A `github_checkout` clone leaves a private repository on a sandbox that is per thread and outlives the turn, so anyone in that thread can later ask gorkie to read those files. Focus mode only seals the turn that holds the credential, not the filesystem afterwards. Approval on the tool means the person agrees to the clone, but not necessarily to the persistence. Either scope the sandbox per user, clear the checkout at turn end, or say plainly on the approval card that the code stays in a shared workspace.
- [ ] Confirm `git config --global --add safe.directory` is genuinely unnecessary on E2B. eve sets it for `/workspace` and the checkout because Vercel Sandbox snapshots are owned by a builder uid that differs from the session uid. E2B appears to run as one user throughout, and the live clone and push probes both worked without it, but that was a fresh sandbox rather than a resumed one. Worth checking against a paused-and-resumed sandbox before relying on it.

- [ ] `github_push_branch` being in the toolset forces sequential tool execution for every step of every turn once GitHub is connected. `effectiveToolSetRequiresSequentialExecution` (`@mastra/core/dist/agent-DSxJoGjY.js:24043`) drops concurrency from the default 10 to 1 when *any* tool in the active set carries `requireApproval`, whether or not it is called. That does close the credential-window race, but it also serialises unrelated sandbox, file, and search calls. Decide whether to accept the cost or gate the tool more narrowly.
- [ ] `processOutputResult` does not run on a thrown or aborted turn, so `sandbox.ts`, `clear-status.ts`, and `turn-footer.ts` all leak on those paths: the sandbox is never paused, the Slack status stays stuck, and no footer posts. Focus mode now covers itself through `onError`/`onAbort` in `defaultOptions`; the same treatment would fix the other three.

- [ ] Live-test the GitHub device flow end to end: click Sign in, enter the code on GitHub, pick one repository, and confirm Slack's Home tab updates itself when the poll lands. Then confirm a read runs unattended, a write pauses for approval, and a repo outside the chosen list reports something better than a bare 404. Nothing here has been run against a real GitHub App yet.
- [ ] Exercise the 8 hour token refresh. `githubAccessToken` refreshes 5 minutes before expiry and disconnects the account when the refresh fails, so a failure is silent apart from a warn log and an App Home that quietly stops saying "Signed in". Worth surfacing that in the GitHub section rather than leaving people to notice.
- [ ] Live-test the approver-identity patch: two people in one thread, gorkie asks A for approval, B clicks Approve. B's click must be ignored and A's must still work. Note B currently gets silence, not a message, since Mastra's handler has no ephemeral path in scope; decide whether that is acceptable or worth widening the patch.
- [ ] Decide what happens when a connected token expires or is revoked. Right now the MCP server just starts failing, the App Home still says "Signed in as X", and nothing tells the person to reconnect. Surfacing the connection error in the GitHub section the way `mcp_servers.last_error` already does for user servers would fix it.
- [ ] `CREDENTIALS_KEY` has no rotation story. Changing it makes every stored GitHub and MCP token undecryptable, and `decryptSecret` would throw on each one rather than prompting a reconnect. At minimum, catch the failure and treat it as disconnected; better, keep a key id in the `v1.` prefix so two keys can be live during a rotation.
- [ ] Verify the plaintext-to-encrypted migration against the real database: existing `mcp_servers.token` rows have no `v1.` prefix, so they pass through `decryptSecret` unchanged and only become ciphertext when that server is next saved. Confirm nothing reads the column outside those helpers, and consider a one-off backfill so no plaintext survives.
- [ ] A user MCP server (Fathom) fails on every reconnect with `invalid_token / No authorization provided` and retries in a loop, filling the log with two stack traces per attempt. Either the stored token is empty or that server wants OAuth rather than a bearer token. Worth backing off after repeated auth failures instead of retrying forever.
- [ ] Consider per-tool overrides on top of the per-server presets, the way the `t3code/mcp-app-home-customization` branch on `imdevarsh/gorkie` does it: a table keyed by user, server, tool, and scope (`global` or `thread`), resolved thread first, then global, then the mode. Worth it only if the three modes prove too coarse in practice. `buildApprovalContext` passes the full request context into the approval function and awaits it, so it needs no patching.
- [ ] Note that "ask only before deleting" still relies on matching tool names against `^(delete|remove)_` for user MCP servers (`approvalFor` in `mcp/user-servers.ts`). GitHub no longer does: it uses the SDK's own `GITHUB_WRITE_TOOLS` list.

- [ ] Live-test the Slack output changes: click a thumbs up and a thumbs down on a turn footer and confirm both record against the right trace, since the click payload shape was never confirmed against a real click. Same turn also exercises the footer's elapsed time and the suggested prompts now coming from adapter config.
- [ ] Live-test the `1.61.0` upgrade: reproduce a Luna rate-limit in Slack and confirm the fallback escalates silently, with a real error still surfacing once both models are exhausted. Extra reason to retest on 1.61: it refactored the response-message-id rotation (`rotateResponseMessageId` now seals the prior message via `messageList`) that the fallback-escalation hunk depends on.
- [ ] Drop the hand-rolled `workspace/filesystem.ts` once `@mastra/e2b` ships a filesystem provider (`mastra-ai/mastra#21875`, open).
- [ ] Handle changing Slack Canvas export sizes in `get_slack_file`.
- [ ] Gate image turns to vision-capable routes. Still the best explanation for broken vision, but narrower than first written, and one claim in the earlier version was wrong. Corrected 2026-09-11: `muse-spark-1.2-contributor` does have vision, so `explorerModels` is fine and the only route in any list without it is `opencode-go/ox-alpha-free`, which sits at position 3 in orchestrator, scout and explorer and is also listed under `deprecatedModels` in the provider registry. Treat the earlier "neither advertises vision" claim as unsound: it was inferred from model naming, not from capability data, and `provider-registry.json` stores the `opencode-go` models as a bare name list with no capability metadata, so nothing here is statically answerable. The mechanism still holds and still fits direct uploads failing: `preferLastWorking` (`providers.ts:30-47`) hoists whichever model last answered to the front for 30 minutes (`config.ts:18`), so a text-only turn landing on `ox-alpha-free` pins the next half hour of image turns to it, and an inlined file part on a user message needs the same capability a tool-result media part does. Owner's call is to leave `providers.ts` alone, so the fix is capability-aware routing at send time, or dropping `ox-alpha-free`, not reshuffling the lists. Confirm first by correlating a failing image turn against the model that served it: `workingModel` persists it, and `processors/working-model.ts` reads `args.result.steps.at(-1)?.response.modelId`.
- [ ] Fix the misleading attachment text in `chat/attachments.ts`. Mastra already inlines png, jpeg, webp and pdf as viewable file parts before we see them, but `:11` overwrites the message with "Slack attachments, not downloaded yet:" and `:24` tells the model to call `get_slack_file`. So the model is told an image it can already see is undownloaded and routes itself through the workspace for no reason. Worth fixing on its own for the wasted round trip, independent of the vision bug.
- [ ] Two smaller image traps, both independent of the routing bug above. `workspace/index.ts:80` registers `read_file` with no `mediaTypes` override, so the default png/jpeg/webp/pdf applies and a GIF, SVG or HEIC comes back as metadata-only text. And code mode's `external_read_file` (`tools/code-mode/slack.ts`) sends its return through `JSON.stringify`, so an image read there is flattened to base64 text unconditionally.
- [ ] Refactor `tools/slack/call-api.ts` and the Slack code mode tool (`tools/code-mode/slack.ts`); the overlap between raw Slack Web API access and the wrapped Slack tools needs a cleaner boundary. The owner's preference is stronger than a refactor: remove `call_slack_api` entirely, add `uploadEmoji`, then reintroduce raw API access only once it is properly secured.
- [ ] Add an `assertCanPostTo`-style destination restriction to `react.ts` (currently the only Slack write tool without one).
- [ ] `slack` (code mode) typing status is a flat "is working in Slack…" regardless of what the generated code actually does. Its input is `{ code: string }` (arbitrary TypeScript calling `external_*` functions), so there's no single field to show like `call_slack_api`'s `method`. Cheap fix: regex the `code` arg for `external_([a-zA-Z0-9_]+)\(` calls and list the distinct function names in the status, same non-invasive pattern already used for `grep`'s `pattern`.
- [ ] Parallel `upload_file` calls can interleave file and comment ordering across concurrent uploads (the `chat` package uploads then posts text sequentially per call, with no cross-call ordering guarantee).
- [ ] A turn can finish generating cleanly (`finishReason: 'stop'`, real usage stats) but the reply never reaches Slack. Likely the streaming/posting pipeline (`chat-channel-render` output processor or the Slack post call itself) failing silently after generation succeeds, not a model/provider issue. Needs a concrete repro thread to trace further.
- [ ] Streaming breaks on scheduled task tools. Needs a concrete repro to trace further.
- [ ] `openai/gpt-5.6-luna` occasionally streams two complete tool calls' JSON concatenated under one tool-call ID (e.g. an `execute_command` call and a `read_file` call glued together with no separator), which Mastra's `tryRepairJson` (`node_modules/@mastra/core/dist/stream-BZ38co-u.js`/`.cjs`; note the fallback-escalation patch lives in the `agent-*` bundles, not this one) can't recover since it only handles single-object malformations (missing quotes, trailing commas, etc.), not multiple concatenated objects. Args come back `undefined`, the tool call fails validation, and the resulting broken message in history can trigger a `400 Bad Request` from whichever provider gets the next request. Same class of provider quirk as the upstream-documented Kimi/K2 issue (`mastra-ai/mastra#11078`). Fix: extend `tryRepairJson` with one more heuristic that detects a `}{` boundary at brace-depth 0 outside strings and keeps just the first balanced object, recovering one tool call instead of losing both.

## Architecture

Six deepening candidates from the 2026-08-26 architecture review. All six agreed; work in this order.

- [ ] Two parts of the above deliberately left alone, decide whether they are worth doing. First, `chat/client.ts` still constructs `SlackAgentAdapter` at import. Socket Mode connects on `initialize()` rather than in the constructor, so this costs an env read and no I/O, and making it lazy means rewriting 39 `slack.` call sites on a live bot for little gain. Second, `chat/instance.ts` still throws rather than holding a test adapter, because with no test suite the second adapter would be hypothetical, and the rule is that one adapter is a hypothetical seam and two is a real one. Both become worth doing the moment a first test exists.

## Ideas

- [ ] Steal eve's handoff artifacts (`agent/lib/artifacts/`). A subagent saves a long finding as a Markdown document under a reserved `artifacts/` prefix and returns only an id; the parent reads it by id instead of carrying the text through context. gorkie's delegation prompt currently just tells children to "tell it where to put anything bulky", which is a convention rather than a mechanism, and `agent-explore` writes files into a sandbox the parent has to go find. eve bounds the body at 200k chars, closes the artifact kinds to a fixed set so the reader knows what shape it holds, and validates the model-supplied id against an anchored pattern with no dots or slashes so a read cannot traverse out of the prefix.
- [ ] Steal eve's reserved-namespace registry (`agent/lib/blob.ts`). One module lists every reserved storage prefix with the tools that own it, and a single guard refuses any general-purpose write that lands inside one, quoting the owning tool in the refusal. gorkie has the same shape of problem in the sandbox filesystem: nothing stops a model-supplied path from overwriting a skill, a checkout, or another thread's work. The leading-slash normalisation is the detail worth copying, since `/artifacts/x` and `artifacts/x` are the same object.
- [ ] Steal eve's per-agent model split (`agent/lib/models.ts`). It gives the station that writes code the strongest coding model and deliberately runs the reviewer on a different vendor so the review is independent of the model that wrote the code. gorkie routes every agent through the same fallback ladder, so `explore` reviewing `orchestrator`'s work is the same model checking itself.
- [ ] Consider eve's `compaction: { thresholdPercent: 0.75 }` and `limits: { maxOutputTokensPerSession }`. gorkie caps steps (`maxSteps: 1000`) and input tokens, but nothing bounds a session's total output, so a runaway turn is only stopped by the step count.
- [ ] Consider a repo-scoped notes document, eve's factory brain (`agent/lib/factory-brain.ts`): durable curated facts about a repository, capped at 40k chars, keyed by a hash of the repo and loaded at the start of work on it. gorkie's observational memory is thread-scoped, so what it learns about a repository in one thread is unavailable in the next.

- [ ] Live-test the GitHub swap end to end. Nothing below has run against Slack or a real repo: that the 34 tools appear for a connected person and none for a disconnected one; that App Home's three presets still gate the right calls (`all` 34/34, `write` 14/34, `delete` 2/34); that a token refreshed mid-conversation is picked up per call; that `github_push_branch` actually pushes and that the credential is dropped afterwards; and that a disconnect mid-turn surfaces as a tool error rather than a silent failure.
- [ ] Decide how a GitHub connection error reaches App Home now. The old MCP path logged and dropped it (`errors[GITHUB_SERVER_NAME]`), and that code is gone; native tool failures surface to the model as tool errors instead, so the Home tab still says "Signed in" while every call fails. `mcp_servers.last_error` has no equivalent for GitHub.

- [ ] Feedback ratings are not deduplicated, so Slack re-dispatching a click records the same thumb twice and skews the aggregates. The observability store cannot answer "has this user already rated this message": `listFeedback` reads local storage, and prod exports Platform-only, so nothing is there to read. Durable dedupe would need our own Postgres table keyed on message and user. Wider than dedupe: the owner reports feedback is completely broken, never reaches Mastra Platform, and is lost once traces expire, so this needs a full revamp rather than a fix. It is coupled to the observability decision in the legacy backlog, since moving to Langfuse or a self-hosted store would change where feedback lands.

- [ ] Skills support: let users add skills from skills.sh, plus custom skills (upload a ZIP, or import from a GitHub gist). Exploratory, not yet scoped.
- [ ] Let channels send rich embeds, starting with the browser. Outbound messages are plain text: `chat/attachments.ts` only covers the inbound direction (downloading a Slack file into the workspace), and every Block Kit builder in the repo sits under `chat/app-home/*/blocks.ts`, which reaches Slack through `publishHome` and so renders the Home tab rather than anything in a conversation. The sandbox already ships `agent-browser`, whose `screenshot <path>` gives a payload for free, so the cheap first version is "here is the page I am looking at" posted inline instead of narrated in prose — which also makes a wrong-page mistake visible to the user immediately rather than three tool calls later. The ambitious version is a live view: `@mastra/browser-viewer` exposes a CDP screencast over a WebSocket, which Slack cannot render inline, so that one needs a hosted viewer URL and an unfurl rather than a true embed. Exploratory, not yet scoped.
- [ ] `taskContext` (`lib/memory.ts`) calls `memoryThread()` on every scheduled-task and wait call, running a Postgres query to derive a thread id the tool already has: channels binds the memory thread as `context.agent.threadId` and its owner as `context.agent.resourceId`. Read those directly in `scheduled-tasks/create.ts`, `list.ts` and `wait.ts`. Keep `memoryThread()` for `chat/commands/stop.ts`, which only has an external thread id. Its `resolvedAgent` and `ctx` return fields have no readers, so the `getAgentById` and `channelContext` calls behind them are dead work too.
- [ ] Decide the fate of the working-model cache. `chat().getState()` resolves to `MastraStateAdapter`, whose `get`/`set` are an in-process `Map`, so `recallModel`/`rememberModel` survive nothing but the current process and the 30-minute TTL is bounded by process lifetime. Either move it to real storage or delete `lib/working-model.ts`, `processors/working-model.ts` and `preferLastWorking()`. Nothing native replaces it.
- [ ] Set `modelSettings.timeout: { stepMs }` on the orchestrator, research and explore agents. Native since 1.60.0, currently unused, and it is the real fix for a provider that opens a stream then stalls, since `stepMs` moves on to the next fallback model.
- [ ] Adopt `ToolSearchProcessor`'s `includeResolvedTools`. Per-user MCP tools come from the dynamic `tools` function, so today they bypass tool search entirely and sit in the prompt on every turn. Caveat: the flag is all-or-nothing over the resolved set, so `orchestratorTools` would become search-only too.
- [ ] Move wait-schedule cleanup to the schedules `onFinish` hook, removing the sweep loop in `scheduled-tasks/wait.ts` and the delete-before-fire ordering question in the `prepare` hook.
- [ ] Drop `validateCron` from `scheduled-tasks/create.ts`: `Schedules.create` validates the expression itself. Keep the minimum-interval check, which has no native equivalent.
- [ ] Delete `await mastra.startWorkers()` from `index.ts` once confirmed no non-server entry point relies on it; the Mastra server already calls it after `listen`.
- [ ] Replace the `sandboxTools` name Set in `processors/sandbox.ts` with a native tool hook (`hooks.beforeToolCall`), which covers every tool including code mode and extends the sandbox before the tool runs rather than after. Needs a Zod narrowing since the hook context is typed `unknown`.
- [ ] Consider `toModelOutput` for the truncation that `tools/slack/call-api.ts` and `tools/grep.ts` currently do inside `execute`, which fuses the model-facing preview with the real output.
- [ ] The `transform.display` summaries on ~25 tools never reach Slack under `toolDisplay: 'hidden'`; the user-visible copy is the separate `chat/status/statuses.ts`. Two parallel per-tool copy systems, only one of which is seen.
- [ ] `types/tools.ts` and `types/tools/` collide, so 31 files must import from `types/tools/index`. Folding the file into the directory removes the suffix everywhere.
- [ ] `TaskToolContext` re-declares a subset of Mastra's `ToolExecutionContext`; `Pick<ToolExecutionContext, 'agent' | 'mastra' | 'requestContext'>` gives the same surface without the hand-written shape.
- [ ] Audited and deliberately kept, do not re-open without new evidence: `tools/grep.ts` (the built-in walks the tree with `readdir` recursion and one `readFile` per candidate over the filesystem provider, and there is still no filesystem-level grep hook to override; re-checked against `1.62.0-alpha.7` on 2026-08-25, byte-identical to `1.61.0` despite #21877 being closed. Note the built-in already caps lines at 500 chars, which is the cap ours was missing until 2026-08-24), `tools/search-web.ts` (native `webSearchTool` throws for the `openrouter` provider), `tools/fetch-url.ts` (native `webFetchTool` returns raw HTML, not extracted article text), `MastraStopCondition`, `processors/clear-status.ts` (no native clear and no turn-end hook), `processors/delegated-tools.ts` (the only thing surfacing sub-agent tool activity), the `adapter.ts` recipient overrides, `chat/app-home/` (channels has no App Home API), and `mcp_servers` staying custom (the `mcpClients` storage domain has no field for a bearer token).
- [ ] Local `check:spelling` cannot run: cspell 10 needs Node >=22.18.0 and the container has 22.17.0 with no version manager, while the repo already pins Node 24 (`.nvmrc`, `engines`). CI runs Node 24 so it is unaffected. Get Node 24 into the dev container.
- [ ] Mastra's `WorkspaceSkills` logs through raw `console.warn`/`console.error` instead of the configured logger, so its warnings bypass pino. Worth filing upstream; the local warning goes away once taste-skill is split.

## Product

- [ ] Refresh `ISSUES.md`. Its header still says it was verified against `@mastra/core@1.59.0`, sections 1.1 (TokenLimiter media) and 1.4 (terminal error chunks) describe hunks that are no longer in the patch, and it does not document the approver-identity gate that is now three of the seven.
- [ ] Replace the `!stop` text command with Slack's native stop button via `agent_session_stopped`. Investigated 2026-09-11. Note the trigger is `!stop`, not `!gorkie stop` (`chat/commands/index.ts:18` matches `/^!(\w+)\b/i` after stripping leading mentions). Nothing supports the event today: zero matches for `agent_session_stopped` anywhere in `node_modules`, and the adapter's dispatch table (`@chat-adapter/slack/dist/index.js:1708-1761`) has no `else`, so the event would be acked and silently dropped. Mastra's channels layer has no cancellation hook either; `ChannelHandlers` is only the four message handlers. So the seam is our own `SlackAgentAdapter`, which already overrides two protected methods, and `processEventPayload` is protected and overridable. Four pieces: add `agent_session_stopped` to `bot_events` in `slack-manifest.json` (currently seven events, and the required `chat:write` scope is already granted); override `processEventPayload` to decode the flat payload and resolve the thread id; factor `stop.ts` into a function taking `{threadId, userId}`, which is easy since everything there already derives from `thread.id`; register it in `chat/events.ts`. The risk to settle first: the button only renders while the session is `processing` via `agents.sessions.setStatus`, and the adapter calls the legacy `assistant.threads.setStatus` (`dist/index.js:3964`). `@slack/web-api@7.19.0` has no `agents.sessions.*` at all, so if the legacy call does not create an agent session the handler will be correct and never fire. Also, the docs want the app to move the session out of `processing` on receipt, so `abortThreadStream` alone may leave the indicator spinning.
- [ ] Add back a thinking display. Slack now has a compact one, so replicate kyto: edit in place, and open a new thinking block past about 50 entries. Check whether the upstream Mastra issue for this has moved.

- [ ] Convert from Socket Mode to webhook mode. No SDK switch needed, confirmed 2026-09-11: `@chat-adapter/slack` declares `type SlackAdapterMode = "webhook" | "socket"` and webhook is its default, while `chat/client.ts:7` explicitly opts into socket. Mastra already builds the receiving half, one POST route per adapter at `/api/agents/{agentId}/channels/{platform}/webhook` (`@mastra/core/dist/channels/agent-channels.d.ts:195-196`), with a stored init promise so handlers can await readiness on a cold start and a `managesRoutes` escape hatch if we would rather own the route. So the work is: drop `mode` and `appToken`, add `signingSecret` plus `SLACK_SIGNING_SECRET` to `src/env.ts` (which today has only `SLACK_BOT_TOKEN` and `SLACK_APP_TOKEN`), and point Slack's Event Subscriptions and Interactivity at the public URL. Prefer `signingSecret` over the `webhookVerifier` option, whose own types carry a SECURITY note that it bypasses built-in verification. The real cost is needing a public URL at all, which is the thing socket mode was buying us.
- [ ] Add OAuth for MCP servers, and finish the GitHub OAuth flow. Blocked on the webhook item above rather than on each other: both need a public redirect URI, which is the shared prerequisite. Note MCP is access-token only today and that was a deliberate call, so this reverses it.
- [ ] Try `connectGithubScopesForTools` from `@github-tools/sdk/connect` to derive the exact GitHub App permissions our 33 tools need, instead of hand-tuning the App config. It is a pure function over the SDK's internal tool catalog (`connect/scopes.ts:177-185`, no `@vercel/connect` import), but the `/connect` barrel also exports `resolveGithubConnector`, which does need `@vercel/connect`. That is an optional peer dep and is not installed, so test that the subpath resolves before relying on it.
- [ ] Per-user rate limiting.
- [ ] Ask for permission before submitting user feedback.
- [ ] Add banning-user support.
- [ ] Rotate the bot token.
- [ ] Run an agent security scan against gorkie.
- [ ] Sign in with GitHub for users.
- [ ] Add a model router.
- [ ] Automatic topic summaries.
- [ ] Auto-add Legacy Gorkie when Gork or Gorkie joins a channel, so joined channels stay tracked.
- [ ] Fix Slack search so it is secure and stays inside Slack's rules. Izie's search PR is the starting point.
- [ ] Do a slop-cleanup review of the GitHub PR, and work through the open GitHub issues. Compaction and observational memory are the worst of them.
- [ ] Image generation that does not depend on HCAI.
- [ ] Add `@gorkie !compact`.
- [ ] Add back the join-thread tool.
- [ ] Find a way to actually enforce the arrow techniques in the prompt.
- [ ] Add a Slack emoji delete operation reachable from the search tool, matching the existing create and upload behaviour, with tests, then open a PR. Emoji creations are logged in the proxy channel as a requested upload followed by a successful creation.
- [ ] Custom Gorkie emojis based on the logo, covering different moods and tasks.
- [ ] Look into Mastra AgentController plus web integration, and decide whether gorkie web is a thing we want.
- [ ] Gork and HuddleFM integration.

## Rewrite parity

The Mastra rewrite absorbed sandboxes, skills, MCP, Slack support and steering. These are the
pieces that have not come back yet.

- [ ] Let gorkie edit existing emojis and upload new ones.
- [ ] Custom providers (BYOK): model and provider choice from models.dev, a $1 per day key usage cap, encrypted at rest. HCAI BYOK is not allowed. Open question worth settling before building: whether people would trust us to hold their keys at all, since the liability may not be worth it.

## Guardrails

- [ ] Never do time-consuming work in the guardrails prompt, for example finding the latest message in every channel.

## Legacy backlog

- [ ] Add MCP servers to Pi (the sandbox) as well.
- [ ] Custom providers via a custom HCAI key, with model choice from models.dev and a $1 per day cap.
- [ ] Consider making gorkie sandbox-first.
- [ ] Better observability: Langfuse plus Logtail, or Langfuse plus Axiom. Decide whether to leave Mastra Platform for Langfuse or something self-hosted, which is the same decision the feedback revamp above needs.
- [ ] Move from TWA's account to Devarsh's account.
- [ ] Run the thermo-nuclear code quality review from `cursor/plugins`.
- [ ] Steering.
